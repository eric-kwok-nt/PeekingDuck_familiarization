input: 
  - "df_records"    # DataFrame for which to be recorded into a csv file
  - "write_now"     # A flag to indicate whether to write the DataFrame to csv before the pipeline ends
  - "pipeline_end"
output: ["none"]  

write_mode: "w"   # Whether to write to a new file or append to an existing file. Defaults to 'w'.
csv_path: "./data/csv/bus_record.csv"
